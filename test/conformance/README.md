# Conformance tests

Every interaction advertised in the CapabilityStatement gets a test here
covering both success and failure: status codes, response types, headers and the
persistence side effects.

The rule these tests enforce is that the CapabilityStatement never claims
something the test matrix does not cover.

Empty until the first interaction ships.
